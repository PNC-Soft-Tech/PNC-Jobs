import ModelList from '@/Components/Models/ModelLists';
import React from 'react';

const index = () => {
    return (
        <div>
            <ModelList/>
        </div>
    );
};

export default index;